{
  "name": "worker-threads",
  "version": "1.0.0",
  "description": "worker threads test",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "dev": "concurrently -k \"tsc --watch\" \"nodemon dist/server.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.15.8",
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.22",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
